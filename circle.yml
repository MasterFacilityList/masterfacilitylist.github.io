
test:
    override:
        - sphinx-build -W -b html -d content/_build/doctrees content content/_build/html


deployment:
    production:
        branch: content
        commands:
          - "bash deployment.sh"
